// Forgix configuration for merging Fabric and Forge JARs
// This file is intentionally minimal - Forgix is not actively used but the plugin is loaded

forgix {
    // Forgix is disabled for this multi-version setup
    // If merging is needed, configure groups here
}
